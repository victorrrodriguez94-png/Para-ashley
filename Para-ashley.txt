<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para Ashley 🩵</title>
    <style>
        body { background-color: #050505; color: white; font-family: 'Arial Rounded MT Bold', sans-serif; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; margin: 0; overflow: hidden; text-align: center; }
        #rain-container, #leclerc-rain { position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 1; }
        .falling-text, .falling-img { position: absolute; animation: fall linear infinite; }
        .falling-text { color: rgba(0, 210, 255, 0.4); font-size: 1rem; white-space: nowrap; }
        .content { z-index: 10; display: flex; flex-direction: column; align-items: center; }
        .name { color: #00d2ff; text-shadow: 0 0 10px #00d2ff; }
        .banner-stage { display: flex; align-items: flex-end; justify-content: center; margin-bottom: 20px; gap: 10px; }
        .char { width: 90px; height: 90px; border-radius: 50%; border: 2px solid white; object-fit: cover; background: white; }
        .pancarta { background: white; color: black; padding: 15px 20px; border-radius: 12px; font-size: 1.2rem; font-weight: 900; border: 3px solid #333; }
        .btn-box { display: flex; gap: 15px; margin-top: 20px; }
        .btn { padding: 12px 25px; border-radius: 50px; border: none; font-weight: bold; cursor: pointer; transition: 0.3s; z-index: 100; }
        .btn-si { background: #00d2ff; color: white; }
        .btn-rojo { background: #ff0000; color: white; }
        .screen { display: none; flex-direction: column; align-items: center; z-index: 20; animation: fadeIn 0.5s; width: 90%; }
        .rosa-text { color: #ff69b4; font-size: 1.6rem; font-weight: 900; text-shadow: 1px 1px 0px #fff; margin-top: 20px; }
        @keyframes fall { to { transform: translateY(110vh); } }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body>
    <div id="rain-container"></div>
    <div id="leclerc-rain"></div>

    <div id="start" class="content">
        <div class="header" style="font-size: 1.5rem; margin-bottom: 20px;">Para la chica más linda: <span class="name">Ashley 🩵</span></div>
        <div class="banner-stage">
            <img src="1000926194.jpg" class="char"> <div class="pancarta">¿quieres salir conmigo?</div>
            <img src="1000926195.jpg" class="char"> </div>
        <div class="btn-box">
            <button class="btn btn-si" onclick="cambiarPantalla('blue')">si</button>
            <button class="btn btn-rojo" onclick="cambiarPantalla('red-step1')">si (pero en rojo)</button>
        </div>
        <img src="1000927037.jpg" style="width: 100px; margin-top: 20px; border-radius: 15px; border: 2px solid white;"> </div>

    <div id="blue" class="screen">
        <img src="1000926260.jpg" style="width: 220px; border-radius: 15px; border: 3px solid #ff69b4;">
        <div class="rosa-text">¡siiii sabía que dirías que siii i love youuu 🩵</div>
    </div>

    <div id="red-step1" class="screen">
        <div class="rosa-text" style="color: #ff0000; text-shadow: none;">porque elegiste este y no el otro? 😠 mentiraaaa siiii vamos a salirrr jijiji i love youuu 🩵</div>
        <button class="btn btn-rojo" style="margin-top:20px; font-size: 1.5rem;" onclick="activarLeclerc()">presionar aquí</button>
    </div>

    <div id="red-final" class="screen">
        <div style="color: #ff0000; font-size: 1.3rem; font-weight: bold; background: white; padding: 10px; border-radius: 10px; margin-bottom: 15px;">te dedico este espacio porque se que amas a charles leclerc ❤️</div>
        <img src="1000927034.jpg" style="width: 280px; border-radius: 20px; border: 3px solid red;">
    </div>

    <script>
        const rain = document.getElementById('rain-container');
        for (let i = 0; i < 30; i++) {
            const div = document.createElement('div');
            div.className = 'falling-text';
            div.innerText = 'te quiero ashley';
            div.style.left = Math.random() * 100 + 'vw';
            div.style.animationDuration = (Math.random() * 3 + 2) + 's';
            rain.appendChild(div);
        }

        function cambiarPantalla(id) {
            document.getElementById('start').style.display = 'none';
            document.querySelectorAll('.screen').forEach(s => s.style.display = 'none');
            document.getElementById(id).style.display = 'flex';
        }

        function activarLeclerc() {
            cambiarPantalla('red-final');
            const leclercRain = document.getElementById('leclerc-rain');
            const fotosLeclerc = ['1000927035.jpg', '1000927036.jpg', '1000927038.jpg'];
            
            for (let i = 0; i < 40; i++) {
                const item = document.createElement('div');
                item.className = 'falling-img';
                if (Math.random() > 0.4) {
                    const imgUrl = fotosLeclerc[Math.floor(Math.random() * fotosLeclerc.length)];
                    item.innerHTML = `<img src="${imgUrl}" style="width:60px; height:60px; border-radius:10px; border: 1px solid red; object-fit: cover;">`;
                } else {
                    item.innerHTML = '<span style="font-size: 2rem;">❤️</span>';
                }
                item.style.left = Math.random() * 100 + 'vw';
                item.style.animationDuration = (Math.random() * 2 + 2) + 's';
                leclercRain.appendChild(item);
            }
        }
    </script>
</body>
</html>
